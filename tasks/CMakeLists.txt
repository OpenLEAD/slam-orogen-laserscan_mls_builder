# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(laserscan_mls_builderTaskLib)
ADD_LIBRARY(${LASERSCAN_MLS_BUILDER_TASKLIB_NAME} SHARED 
    ${LASERSCAN_MLS_BUILDER_TASKLIB_SOURCES})
add_dependencies(${LASERSCAN_MLS_BUILDER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${LASERSCAN_MLS_BUILDER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${LASERSCAN_MLS_BUILDER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${LASERSCAN_MLS_BUILDER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LASERSCAN_MLS_BUILDER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LASERSCAN_MLS_BUILDER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LASERSCAN_MLS_BUILDER_TASKLIB_HEADERS}
    DESTINATION include/orocos/laserscan_mls_builder)

