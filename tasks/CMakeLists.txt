# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(accumulated_pointcloudTaskLib)
ADD_LIBRARY(${ACCUMULATED_POINTCLOUD_TASKLIB_NAME} SHARED 
    ${ACCUMULATED_POINTCLOUD_TASKLIB_SOURCES})

add_dependencies(${ACCUMULATED_POINTCLOUD_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ACCUMULATED_POINTCLOUD_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ACCUMULATED_POINTCLOUD_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ACCUMULATED_POINTCLOUD_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ACCUMULATED_POINTCLOUD_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ACCUMULATED_POINTCLOUD_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ACCUMULATED_POINTCLOUD_TASKLIB_HEADERS}
    DESTINATION include/orocos/accumulated_pointcloud)

